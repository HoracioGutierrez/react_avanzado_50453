const initialState = {
    data : {
        nombre : "",
        apellido : ""
    },
    islogged : false
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}
